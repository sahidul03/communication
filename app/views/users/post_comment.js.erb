if("<%= @post_comment_flag%>"=="true"){
    $('.single_post').each(function(){
        var post_id=$(this).data('postid');
        if(post_id=="<%= @post.id %>"){
            $(this).find('.comment_container').find('.comments').append('<%= escape_javascript render("users/post_comment") %>');
        }
    });
}
